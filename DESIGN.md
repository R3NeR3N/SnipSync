# DESIGN.md — UI/UX デザイン指標

> SnipSync の見た目・操作感を一貫させるためのデザインシステム。UI を変える際の判断基準。
> 値は `src/app.py` の実装と対応している（変更時は両方そろえる）。

---

## 1. デザイン原則

1. **ドロップして待つだけ** — 主要動線は「D&D → 設定確認 → 1 ボタン実行」。迷わせない。
2. **ダーク・集中** — 暗色で映像素材のサムネ/作業に馴染ませる。明色テーマは持たない。
3. **進捗を隠さない** — 処理は外部プロセス任せで時間がかかる。ログ＋プログレスバーで「動いている」を常に可視化。
4. **非破壊・安心感** — 元動画を壊さないことを文言・挙動で担保（完了ダイアログ／出力先明示）。
5. **二言語等価** — ja/en で情報量・レイアウトを同等に保つ。

---

## 2. カラーパレット

`src/app.py` 定義の定数と一致させること。

| 役割 | 定数 | 値 | 用途 |
|---|---|---|---|
| アクセント | `ACCENT` | `#6C63FF` | 主ボタン・スライダー・強調 |
| アクセント(hover) | `ACCENT_HOVER` | `#5a52e0` | ホバー時 |
| 成功 | `SUCCESS` | `#2ECC71` | 完了ログ |
| エラー | `ERROR_COL` | `#E74C3C` | 失敗ログ・停止ボタン hover |
| 警告 | `WARN_COL` | `#F39C12` | 注意ログ |
| 背景(ベース) | `BG_DARK` | `#1a1a2e` | ウィンドウ地・メニュー地 |
| 背景(カード) | `BG_CARD` | `#16213e` | ヘッダ・設定カード・ドロップゾーン |
| 背景(コンソール) | `BG_CONSOLE` | `#0d1117` | ログ領域 |
| 補助テキスト | `TEXT_MUTED` | `#8892a4` | ラベル・補足・muted ログ |
| 本文テキスト | （直値） | `#c9d1d9` | コンソール本文 |
| ログ:info | （tag） | `#58a6ff` | 情報ログ |

> 新色を足すときは直値で散らさず、必ず定数化（将来 `ui/theme.py` へ集約）。

---

## 3. タイポグラフィ

| 用途 | フォント | サイズ/ウェイト |
|---|---|---|
| アプリタイトル | Segoe UI | 22 / bold |
| 見出しラベル | Segoe UI | 12 / bold |
| 補助ラベル | Segoe UI | 11 / regular |
| 本文・サブタイトル | Segoe UI | 12–13 |
| 数値・コマンド・ログ | Consolas | 11–13 |

- UI テキストは **Segoe UI**、数値/パス/ログなど等幅が要る箇所は **Consolas** で統一。

---

## 4. レイアウト・スペーシング

| 項目 | 値 |
|---|---|
| ウィンドウ初期サイズ | `860 × 780` |
| 最小サイズ | `740 × 720` |
| ヘッダ高さ | `64px` |
| カード角丸 | `16`（ドロップゾーン/設定）/ `12`（ボタン・コンソール） |
| 主要外周パディング | `padx 20 / pady 16` |
| 設定行パディング | `padx 20 / pady 8` |
| 実行ボタン高さ | `50px` |

### 画面構成（上→下）
```
┌────────────────────────────────────────┐
│ ヘッダ: ✂ SnipSync  サブ見出し   [言語▼] │
├────────────────────────────────────────┤
│ ドロップゾーン（🎬 クリック/D&D）        │
│ 設定カード                              │
│   ・無音マージン  [slider][値][入力]     │
│   ・音量閾値     [slider][値][入力]     │
│   ・出力形式     [▼]                    │
│   ・字幕生成     [☑] モデル[▼]          │
│   ・出力先       パス        [変更]      │
│ [▶ 処理を開始する        ] [⬛ 停止]      │
│ プログレスバー（indeterminate）          │
│ ログ                          [クリア]   │
│ ┌── コンソール（色分けログ）──────────┐ │
│ └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

---

## 5. インタラクション規約

- **スライダーと数値入力は双方向同期**（マージン/閾値）。入力はクランプ（マージン 0.0–2.0、閾値 0.0–100.0）。
- **処理中**: 実行ボタン disabled・停止ボタン enabled・プログレスバー start。終了で逆へリセット。
- **字幕 OFF**: モデル選択メニューを disabled。
- **faster-whisper 未導入**: 字幕チェック/モデル選択を disabled にし、警告ログを 1 行出す。
- **ログ色分け**: info=青 / success=緑 / error=赤 / warn=橙 / muted=灰。種別を必ずタグで渡す。
- **完了時**: 「出力フォルダを開きますか？」ダイアログ → Yes で `os.startfile`。

---

## 6. アイコン・トーン

- 絵文字を機能アイコンとして軽く使用（✂ ロゴ / 🎬 入力 / ▶ 実行 / ⬛ 停止 / ✅ 成功 / ❌ 失敗 / ⏳ 進行）。
- 文言は簡潔・行動指向（「▶ 処理を開始する」など）。専門語は最小限、初心者にも通じる表現。

---

## 7. やってはいけない

- ❌ ライトテーマ追加（ダーク固定が世界観）。
- ❌ アクセント色を `#6C63FF` 以外に散らす。
- ❌ 等幅が必要な数値/パスを Segoe UI で表示。
- ❌ 処理の進捗を隠す（ログ/プログレスを省略しない）。
