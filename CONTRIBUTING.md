# CONTRIBUTING.md — 開発・協業ガイド

> 本プロジェクトは **AI 協業（頭脳 × 作業の分業）** で開発する。
> 元方針: `Dev-References-Global/AI Collaboration Policy.md`。エージェント行動ルールは AGENTS.md が正典。

---

## 1. モデルの役割分担

| 役割 | モデル | 担当領域 |
|---|---|---|
| 🧠 頭脳 | **Claude Opus 4.8** | 要件定義・設計・タスク分割・最終レビュー・エスカレーション対応 |
| 🔧 作業 | **Gemini 3.5** | 実装・テスト実装・大量コード生成・機械的リファクタ・ドキュメント生成 |

原則:
- **設計と最終判断は必ず頭脳**。頭脳に定型コード/単純実装を書かせない。
- **大量・定型の実装は作業**へ。安く速く回す。
- どちらに振るか曖昧なタスクは、まず頭脳に「設計＋分割」だけさせ、実装を作業へ渡す（🧠→🔧）。

---

## 2. 開発ワークフロー（自律ファクトリー型）

```
① 要件定義・設計 [🧠]
     目的・制約・「完成の条件」を1ターンで受け取り、設計書を作る。
     作業AIが迷わない粒度までタスク分割。
        ↓
② 実装 [🔧]
     設計書をもとに本体コードを実装。
        ↓
③ テスト実装 [🔧]   ※実装コードではなく「設計書」から書く
     実装のバグを正解扱いしないため、別タスクとして作成。
        ↓
④ 自動テスト＆自己修正ループ [🔧 ↔ 実行環境]
     テスト実行 → 失敗ならログを読み自己修正（上限3回）。
     3回で直らなければ頭脳へエスカレーション。
        ↓
⑤ 最終レビュー [🧠]   ※テスト通過コードのみ
     文法ではなく設計意図とのズレ・脆弱性・拡張性を見る。
     確信が低くても全件報告（重要度判定は別工程）。
        ↓
⑥ ドキュメント生成 [🔧]   ※テスト通過後
     CHANGELOG / docs などを更新。
```

### 禁止事項
- ❌ 頭脳に定型コード・単純実装を書かせる。
- ❌ 自動テストを通さずに頭脳のレビューへ回す。
- ❌ 依存関係のある工程を無理に並列化する（マージ衝突の元）。
- ❌ テストを実装コードから書く（設計書から書く）。

---

## 3. 設計書とプロンプトキャッシュ運用

作業 AI（Gemini 3.5）側でキャッシュを効かせるための規律。

- **同一の設計書を、各作業リクエストの「先頭」に毎回そのまま付ける**（外部参照ではなく前置きの再利用）。
- 設計書は**途中で書き換えない**。1 バイト変わると以降のキャッシュが全無効。修正は版を分けるか末尾へ追記。
- 作業ごとに変わる個別指示は設計書の**後ろ**に置く。
- キャッシュ発動条件（①完全一致 ②先頭固定 ③最小サイズ超 ④TTL 内）は提供元ごとに違う。Gemini 側の最小トークン数・TTL を事前確認（小さい設計書は発動しないことがある）。
- 並列化は**依存のない独立タスク限定**。
- 1 タスクあたりのトークン上限（予算）を決め、暴走を防ぐ。

> 本プロジェクトの「共有設計書」相当は AGENTS.md + CONTEXT.md + ARCHITECTURE.md。
> 作業依頼時はこれらを前置きとして固定し、個別タスク指示を後置する。

---

## 4. SnipSync 固有の開発ルール（AGENTS.md からの要約）

- 変更ごとに `python src/app.py` を起動し**目視確認**するまで完了としない（GUI アプリ）。
- 文字列は必ず `I18N` 経由、`ja`/`en` 両方追加。
- パスは `Path(...).resolve()`。subprocess は `encoding="utf-8", errors="replace"`。
- 意思決定は MEMORY.md、失敗手順は PITFALLS.md に追記。着手前に PITFALLS.md を一読。
- バージョンに触れたらコード・README・CHANGELOG を同期（AGENTS.md §5）。

---

## 5. Git / GitHub 運用方針

AI 協業（頭脳 × 作業）を前提とした、ソロ開発向けの軽量 trunk 運用。

### 5.1 ブランチ戦略（軽量 trunk + タスクブランチ）
- `main` = 常にビルド可能・テスト green の幹。**直接 push しない**。
- 作業はタスク単位（ROADMAP の1項目）でブランチを切る。
  - `feat/<topic>` / `fix/<topic>` / `refactor/<topic>` / `docs/<topic>` / `test/<topic>`
  - 例: `fix/version-unify`、`fix/whisper-auto-lang`、`test/p0-regression`
- **1 ブランチ = 1 論理タスク**。依存のない独立タスクは別ブランチで並行可。依存タスクは直列（§2 禁止事項）。

### 5.2 コミット
- Conventional Commits（`feat:` `fix:` `docs:` `refactor:` `test:` `build:` `chore:`）。
- 1 コミット = 1 論理変更。生成物（`build/` `dist/` `venv/`）・モデル（`*.bin` `*.pt`）は含めない。
- push 前に `git status` で生成物混入を確認（PITFALLS.md P-5 / P-7）。

### 5.3 PR = 頭脳レビューのゲート
- 作業役（Gemini）はブランチ内で実装し、**自動テストが green になって初めて** `main` 向け PR を作成する。
- PR 本文の必須項目: 対象 ROADMAP 項目 / 変更概要 / `pytest` 出力 / `python src/app.py` 起動確認結果。
- **頭脳（Opus 4.8）が PR をレビュー**（設計意図とのズレ・脆弱性・拡張性を全件報告）→ 通過後に merge。これが §2 ⑤「最終レビュー」の実体。
- テスト未通過コードを PR に上げない（§2 禁止事項）。
- merge 方式は **Squash merge** を推奨（`main` 履歴を論理単位で保つ）。

### 5.4 リリース（タグ + GitHub Releases）
- `APP_VERSION`・git tag・README バッジの **3 点を必ず一致**させる（AGENTS.md §5）。tag は SemVer（例 `v1.0.0`）。
- 手順: `CHANGELOG.md` の `Unreleased` を確定 → tag `vX.Y.Z` を打つ → GitHub Release 作成 → `dist/SnipSync.exe` を **アセットとして添付**（リポジトリにはコミットしない）。
- Release note は CHANGELOG の該当節を流用。

### 5.5 AI 作業役の制約
- 作業役は**ブランチ内で完結**。`main` の直接操作・force push 禁止。
- 対話型 Git コマンド（`git rebase -i`、`git add -i` 等）は使わない（環境非対応）。
- 破壊的操作（`reset --hard` / force push）は頭脳承認時のみ。

> ソロ運用のため branch protection は任意。厳格運用なら GitHub 側で `main` に「PR 必須 + status check（pytest）green 必須」を設定する。
